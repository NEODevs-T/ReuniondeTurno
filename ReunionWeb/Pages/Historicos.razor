@page "/es/historicos"
@page "/en/historical"

@using Radzen.Blazor
@inject IAsistenciaReuData AsistenciaReuData
@inject IAvisadorData AvisadorData
@inject ICargoReuData CargoReuData
@inject IKsfData KsfData
@inject IPizarraData PizarraData
@inject IRespoReuData RespoReuData
@inject ICausaCalidadData CausaCalidadData
@inject IMaestraData MaestraData
@inject NavigationManager NavigationManager
@inject HttpClient Http
@inject AuthenticationStateProvider AuthStateProvider
@inject ILocalStorageService LocalStorage
@inject NotificationService NotificationService
@inject IDiccionarioTraduccionesService DiccionarioTraduccionesService
@inject LocalizationService LocalizationService
@inject ITranslationService Translator


@using ReunionWeb.Resources
@using System.Globalization
@using ReunionWeb.Services




<BSContainer>

    @if (!traduccionesCargadas || MaestraData.centro.Count() == 0)
    {
        <div class="loader"></div>
    }
    else
    {
<h3>@DiccionarioTraduccionesService.Traducciones["Historicos"]</h3>

        <BSRow>
            <BSCol Column="3" ColumnSmall="2">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Centro"]</BSLabel>
                <BSInput InputType="InputType.Select" @onchange="BuscarDivision" @bind-Value="IdCentro">
                    <option value="0">@DiccionarioTraduccionesService.Traducciones["Seleccione"] </option>
                    @foreach (var d in MaestraData.centro)
                    {
                        <option value="@d.IdCentro">@d.Centro</option>
                    }
                </BSInput>
            </BSCol>


            <BSCol Column="3" ColumnSmall="2">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Division"]</BSLabel>
                <BSInput InputType="InputType.Select" @bind-Value="IdDiv">
                    <option value="0">@DiccionarioTraduccionesService.Traducciones["Seleccione"]</option>
                    @foreach (var d in MaestraData.divisions)
                    {
                        <option value="@d.IdDivision">@d.Ndivision</option>
                    }
                </BSInput>
            </BSCol>



            <BSCol Column="3" ColumnSmall="2">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Desde"]</BSLabel>
                <BSInput InputType="InputType.Date" @bind-Value="fecha1" />
            </BSCol>

            <BSCol Column="3" ColumnSmall="2">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Hasta"]</BSLabel>
                <BSInput InputType="InputType.Date" @bind-Value="fecha2" />
            </BSCol>

            <BSCol ColumnSmall="2" Column="3">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Codigo"]</BSLabel>
                <BSInput InputType="InputType.Select" IsDisabled @bind-Value="Codigo">
                    <option value="Todo">@DiccionarioTraduccionesService.Traducciones["EnCurso"]</option>
                </BSInput>
            </BSCol>

            <BSCol ColumnSmall="2" Column="3">
                <BSLabel>@DiccionarioTraduccionesService.Traducciones["Estado"]</BSLabel>
                <BSInput InputType="InputType.Select" @bind-Value="Status">
                    <option value="Todo">@DiccionarioTraduccionesService.Traducciones["Todo"]</option>
                    <option value="Pendiente">@DiccionarioTraduccionesService.Traducciones["Pendiente"]</option>
                    <option value="Cerrado">@DiccionarioTraduccionesService.Traducciones["Cerrado"]</option>
                </BSInput>
            </BSCol>

        </BSRow>
        <p>
        </p>
        <BSRow>

            


            @* <BSCol ColumnSmall="2" Column="1">
                <BSRow>
                    <BSLabel> Filtro Fecha de Trabajo</BSLabel>
                </BSRow>
                <BSInputRadio CheckedValue="@("2")" @bind-Value="tipoconsulta" IsToggle="true" Size="Size.ExtraSmall"
                    Color="BSColor.Success" IsOutlined="true">
                    Fecha Trabajo
                    <span class="oi oi-cog"></span>
                </BSInputRadio>
            </BSCol> *@

            <BSCol ColumnSmall="2" Column="1">
                <BSRow>
                    <BSLabel>@DiccionarioTraduccionesService.Traducciones["FiltroFechaDeReunion"]</BSLabel>
                </BSRow>
                <BSInputRadio CheckedValue="@("0")" @bind-Value="tipoconsulta" IsToggle="true" Size="Size.ExtraSmall"
                    Color="BSColor.Success" IsOutlined="true">
                    @DiccionarioTraduccionesService.Traducciones["FReunion"]
                    <span class="oi oi-people"></span>
                </BSInputRadio>
            </BSCol>

            <BSCol Column="3" ColumnSmall="2">
    <BSLabel>@DiccionarioTraduccionesService.Traducciones["DetallesDeCalidad"]</BSLabel>
    <BSInput InputType="InputType.Select" @bind-Value="SelectedCausaCalidad">
        <option value="0">@DiccionarioTraduccionesService.Traducciones["Todo"]</option>
        @foreach (var d in CausaCalidadData.causaCals)
        {
            var texto = idPais == 5? d.Ccenglish : d.Ccnombre;
            <option value="@d.IdCausaCal">@texto</option>
        }
    </BSInput>
</BSCol>
            <BSCol ColumnSmall="2" Column="2">
                <BSRow>
                    <BSLabel>@DiccionarioTraduccionesService.Traducciones["Buscar"]</BSLabel>
                </BSRow>
                <BSButton IsOutlined Color="BSColor.Success" Size="Size.ExtraSmall" OnClick="Get"> @DiccionarioTraduccionesService.Traducciones["Consultar"] <span
                        class="oi oi-check"></span></BSButton>
            </BSCol>

        </BSRow>
    }

</BSContainer>

<p></p>

@if (tabla.Count() > 0 && dataIsLoaded == 2)
{
<div class="scrollable-container">
    <RadzenDataGrid @ref="@ReuTabla" Data="@PizarraData.reudiatablas" class="rz-shadow-2" AllowFiltering="true" AllowColumnResize="true"
        AllowAlternatingRows="true" FilterMode="FilterMode.Advanced" AllowSorting="true" PageSize="30" AllowPaging="true"
        PagerHorizontalAlign="HorizontalAlign.Left" ShowPagingSummary="true" TItem="ReunionDTO"
        LogicalFilterOperator="LogicalFilterOperator.Or" OrOperatorText="O" AndOperatorText="Y" EqualsText="Igual a"
        EditMode="DataGridEditMode.Single" ColumnWidth="200px">
        <Columns>
            @*
        <RadzenDataGridColumn TItem="ReunionDTO" Title="Centro" Frozen="true" Property="Rdcentro" Width="80px">
        <Template>
        @context.Rdcentro
        </Template>

        </RadzenDataGridColumn>

        <RadzenDataGridColumn TItem="ReunionDTO" Title="División" Frozen="true" Property="Rddiv" Width="80px">
        <Template>
        @context.Rddiv
        </Template>
        </RadzenDataGridColumn>*@

            <RadzenDataGridColumn TItem="ReunionDTO" Title="#" Width="40px" Filterable=false>
                <Template>

                    @(tabla.IndexOf(context) + 1)
                </Template>
            </RadzenDataGridColumn>
            <RadzenDataGridColumn TItem="ReunionDTO" Property="Rdarea" Width="120px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span stytle="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["Linea"]</span>
                </HeaderTemplate>
                <Template>
                    @context.Rdarea
                </Template>
            </RadzenDataGridColumn>



            <RadzenDataGridColumn TItem="ReunionDTO" Property="RdcodEq" Width="180px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["CodigoDeEquipo"]</span>
                </HeaderTemplate>
                <Template>
                    @context.RdcodEq
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="RdcodEq" Width="180px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["NombreDelEquipo"]</span>
                </HeaderTemplate>
                <Template>
                    @(string.IsNullOrWhiteSpace(context.EnombreEam) ? (idPais == 5 ? "EQUIPMENT NOT FOUND" : "EQUIPO NO ENCONTRADO") : context.EnombreEam)
                    
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="Ksf" Width="100px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">KSF</span>
                </HeaderTemplate>
                <Template>
                    @context.Ksf
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="Rddisc" Width="220px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["Discrepancia"]</span>
                </HeaderTemplate>
                <Template>
                    @(context.Rddisc?.ToUpperInvariant() ?? string.Empty)
                </Template>
            </RadzenDataGridColumn>

            @* <RadzenDataGridColumn TItem="ReunionDTO" Title="Código" Property="RdcodDis" Width="60px">
                <Template>
                    @context.RdcodDis
                </Template>
            </RadzenDataGridColumn> *@

            <RadzenDataGridColumn TItem="ReunionDTO" Property="RdplanAcc" Width="200px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["Accion"]</span>
                </HeaderTemplate>
                <Template>
                    @context.RdplanAcc
                </Template>
            </RadzenDataGridColumn>

            @* <RadzenDataGridColumn TItem="ReunionDTO" Title="Tiempo" Property="Rdtiempo" Width="60px">
                <Template>
                    @context.Rdtiempo
                </Template>
            </RadzenDataGridColumn> *@

            <RadzenDataGridColumn TItem="ReunionDTO" Property="Responsable" Width="150px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["Responsable"]</span>
                </HeaderTemplate>
            <Template>
                @context.Responsable
            </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="CausaCalNombre" Width="180px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["DetallesDeCalidad"]</span>
                </HeaderTemplate>
                <Template>
                    @context.CausaCalNombre
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="OrigenCal" Width="180px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["OrgDelProducto"]</span>
                </HeaderTemplate>
                <Template>
                    @context.OrigenCal.ToUpperInvariant()
                </Template>

            </RadzenDataGridColumn>
            <RadzenDataGridColumn TItem="ReunionDTO" Property="Rdstatus" Width="120px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["Estado"]</span>
                </HeaderTemplate>
                <Template>
                    @{
                        var clave = context.Rdstatus ?? string.Empty;

                        var traduccion = DiccionarioTraduccionesService.Traducciones.TryGetValue(clave, out var valor)
                            ? valor
                            : clave;
                    }
                    <span>@traduccion</span>
                </Template>
            </RadzenDataGridColumn>

            <RadzenDataGridColumn TItem="ReunionDTO" Property="RdfecReu" Width="130px" TextAlign="TextAlign.Center">
                <HeaderTemplate>
                    <span style="white-space: nowrap;">@DiccionarioTraduccionesService.Traducciones["FReunion"]</span>
                </HeaderTemplate>
                <Template>
                    @context.RdfecReu.ToString("dd/MM/yyyy")
                </Template>
            </RadzenDataGridColumn>

            @* <RadzenDataGridColumn TItem="ReunionDTO" Title="F. Trabajo" Property="RdfecTra" Width="90px">
                <Template>
                    @context.RdfecTra.ToString("yyyy/MM/dd")
                </Template>
            </RadzenDataGridColumn> *@



        </Columns>
    </RadzenDataGrid>
    </div>
}
else if (dataIsLoaded == 1 && tabla.Count() == 0)
{
    <div class="loader"></div>
}
else if (dataIsLoaded == 2 && tabla.Count() == 0)
{
<div style="text-align: center; padding: 20px; font-size: 18px; color: rgb(102, 101, 101);">
    <p>@DiccionarioTraduccionesService.Traducciones["NoHayDatos"]</p>
</div>
}

@code {
    RadzenDataGrid<ReunionDTO>? ReuTabla;
    List<ReunionDTO> tabla = new List<ReunionDTO>();

    int dataIsLoaded = 0;
    private bool data { get; set; } = false; //Centro


    [Required(ErrorMessage = "Seleccione el Centro")]
    private int IdCentro { get; set; } = 0; //id del Centro
    [Required(ErrorMessage = "Seleccione la división.")]
    private int IdDiv { get; set; } = 0; //id de la division

    private int SelectedCausaCalidad { get; set; } = 0;

    //private int CentroComedor;//Variable para verificar si es el comedor venezuela;
    private string errorMessage = string.Empty; //variable para que aparezca mensaje

    //private string Div { get; set; } //Centro
    //private string Division { get; set; } //División

    private string Codigo { get; set; } = "Todo"; //Codigo
    private string Status { get; set; } = "Todo"; //Status
    private string UsrAdmin { get; set; } = ""; //permisos de admin
    private string UsrRol { get; set; } = ""; //rol para filtrar
    private bool cargandocambio { get; set; } = false; //para cargar los datos del trazabilidad
    private string tipoconsulta { get; set; } = "0"; //1-fecha de reuinion 0 fecha trabajo
    private NotifiRadzen Notificar = new NotifiRadzen();//Logic para crear las noyificaciones radzen
    private bool traduccionesCargadas = false;
    private List<Claim> claims { get; set; } = new();
    public DateTime fecha1;
    public DateTime fecha2;

    //private string mensaje = "";
        [CascadingParameter]
    private Task<AuthenticationState>? authenticationState { get; set; }
    private int idPais { get; set; }

    private async Task CreateData()

    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        claims = Enumerable.ToList(user.Claims);

        var idPaisClaim = claims.FirstOrDefault(c => c.Type == "IdPais")?.Value;
        if (int.TryParse(idPaisClaim, out int paisTemp))
        {
            idPais = paisTemp;
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        fecha1 = DateTime.Now;
        fecha2 = DateTime.Now;
        await CreateData();
        await LocalizationService.EstablecerCulturaDesdeTokenAsync();
        CultureInfo.CurrentCulture = CultureInfo.DefaultThreadCurrentCulture ?? CultureInfo.InvariantCulture;
        CultureInfo.CurrentUICulture = CultureInfo.DefaultThreadCurrentUICulture ?? CultureInfo.InvariantCulture;
        await DiccionarioTraduccionesService.CargarTraduccionesAsync();
        traduccionesCargadas = true;


        var role = claims.FirstOrDefault(c => c.Type.Contains("role"))?.Value
            ?? string.Empty;
        var idEmpresa = claims.FirstOrDefault(c => c.Type.Contains("IdEmpresa"))?.Value
            ?? string.Empty;
        var idCentro = claims.FirstOrDefault(c => c.Type.Contains("IdCentro"))?.Value
            ?? string.Empty;

        switch (role)
        {
            case "Admin":
            case "SuperAdmin":
                UsrAdmin = "SuperAdmin";
                await MaestraData.GetCentrosxEmpresa("All" + idEmpresa);
                break;

            case "SemiAdmin":
                UsrAdmin = "SemiAdmin";
                await MaestraData.GetCentrosxEmpresa(idCentro);
                break;

            case "Planificador":
            case "SuperUser":
                await MaestraData.GetCentrosxEmpresa(idCentro);
                break;

            case "MttoAdmin":
                UsrRol = "Mtto";
                await MaestraData.GetCentrosxEmpresa("All" + idEmpresa);
                break;

            case "SeguridadAdmin":
                UsrRol = "Seguridad";
                await MaestraData.GetCentrosxEmpresa("All" + idEmpresa);
                break;

            case "CalidadAdmin":
                UsrRol = "Calidad";
                await MaestraData.GetCentrosxEmpresa("All" + idEmpresa);
                break;

            case "Mantenimiento":
                UsrRol = "Mtto";
                await MaestraData.GetCentrosxEmpresa(idCentro);
                break;

            case "Auditores":
                await MaestraData.GetCentrosxEmpresa("All" + idEmpresa);
                break;

            default:
                UsrRol = role;
                await MaestraData.GetCentrosxEmpresa(idCentro);
                break;
        }


        await KsfData.Getksf();
        await RespoReuData.GetResReu();
        await CausaCalidadData.GetCausasCalidad();

        PizarraData.reudiatablas = new List<ReunionDTO>();


    }
    
    protected async Task BuscarDivision(ChangeEventArgs e)
    {
        IdDiv = 0;

        if (e?.Value is not string valor || !int.TryParse(valor, out var centroId))
        {
            IdCentro = 0;
            return;
        }

        IdCentro = centroId;

        if (IdCentro == 0)
            return;

        try
        {
            MaestraData.divisions =
                await MaestraData.ObtenerDivisionDelCentro(IdCentro);
        }
        catch
        {
            NotificationService.Notify(
                Notificar.Notificacion(
                    "error",
                    "Error",
                    DiccionarioTraduccionesService.Traducciones["SCentro"]
                )
            );
        }
    }

    protected async Task Get()
    {
        if (IdCentro == 0)
        {
            NotificationService.Notify(Notificar.Notificacion("advertencia", DiccionarioTraduccionesService.Traducciones["Atencion"],DiccionarioTraduccionesService.Traducciones["SVCentro"]));
            return;
        }

        if (IdDiv == 0)
        {
            NotificationService.Notify(Notificar.Notificacion("advertencia", DiccionarioTraduccionesService.Traducciones["Atencion"],DiccionarioTraduccionesService.Traducciones["SVDivision"]));
            return;
        }

        if (dataIsLoaded != 1)
        {
            dataIsLoaded = 1;
            tabla = await PizarraData.GetHistoricos(IdCentro.ToString(), IdDiv.ToString(), fecha1, fecha2, tipoconsulta, Status);
foreach (var d in PizarraData.reudiatablas)
{
    // KSF
    d.Ksf = KsfData.ksfs
        .FirstOrDefault(x => x.Idksf == d.Idksf) is var ksf && ksf != null
            ? (idPais == 5 ? ksf.KsfEnglish : ksf.KsfNombre)
            : string.Empty;

    // Responsable
    d.Responsable = RespoReuData.resporeus
        .FirstOrDefault(x => x.IdResReu == d.IdResReu) is var respo && respo != null
            ? (idPais == 5 ? respo.Rrenglish : respo.Rrnombre)
            : string.Empty;

    // Causa de calidad
    var causa = CausaCalidadData.causaCals
        .FirstOrDefault(x => x.IdCausaCal == d.IdCausaCal);

    if (causa == null)
    {
        d.CausaCalNombre = idPais == 5
            ? "Cause not found"
            : "Causa no encontrada";
        continue;
    }

    // ✅ async correctamente awaited
    await MaestraData.GetPRueba01(causa.IdCausaCal);

    d.CausaCalNombre = idPais == 5
        ? causa.Ccenglish
        : causa.Ccnombre;
}
               {
        if (SelectedCausaCalidad == 0)
        {
            // Mostrar todo
            PizarraData.reudiatablas = PizarraData.reudiatablas; // Asegúrate de mantener una copia sin filtrar
        }
        else
        {
            // Filtrar por IdCausaCalidad
            PizarraData.reudiatablas = PizarraData.reudiatablas
                .Where(x => x.IdCausaCal == SelectedCausaCalidad)
                .ToList();
        }
    }
   

            dataIsLoaded = 2;
    }
    

}


}